import { entryIdFilterPipe } from './entryidfilter.pipe';

describe('entryIdFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new entryIdFilterPipe();
    expect(pipe).toBeTruthy();
  });
});